name: Claude Jira
on:
  workflow_dispatch:
    inputs:
      ticket_id:
        description: 'Jira ticket ID (e.g., TASKS-123)'
        required: true
        type: string
      summary:
        description: 'Ticket summary'
        required: true
        type: string
      description:
        description: 'Ticket description'
        required: true
        type: string
  repository_dispatch:
    types: [jira-in-progress]
jobs:
  implement:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    uses: Juzinn/claude-workflows/.github/workflows/claude-jira.yml@main
    with:
      ticket_id: ${{ github.event.inputs.ticket_id || github.event.client_payload.issue_key }}
      summary: ${{ github.event.inputs.summary || github.event.client_payload.issue_summary }}
      description: ${{ github.event.inputs.description || github.event.client_payload.issue_description }}
      install_command: 'pip install uv && uv sync'
      allowed_tools: 'Read,Edit,Write,Grep,Glob,Bash(uv sync:*),Bash(uv run pytest:*),Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git checkout:*),Bash(git branch:*),Bash(git diff:*),Bash(git log:*)'
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      CI_GITHUB_APP_ID: ${{ secrets.CI_GITHUB_APP_ID }}
      CI_GITHUB_APP_PRIVATE_KEY: ${{ secrets.CI_GITHUB_APP_PRIVATE_KEY }}
